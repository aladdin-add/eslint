#!/usr/bin/env node

/**
 * @fileoverview Main CLI that is run via the eslint --init command.
 * @author 唯然<weiran.zsd@outlook.com>
 */

/* eslint no-console:off */

"use strict";

require("../lib/config-initializer").initializeConfig();
